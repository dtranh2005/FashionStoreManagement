<Window x:Class="FashionStoreManagement.ProductsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FashionStoreManagement" 
        xmlns:Icon="clr-namespace:MahApps.Metro.IconPacks;assembly=MahApps.Metro.IconPacks.Material"
        mc:Ignorable="d"
        Title="MainWindow" Height="720 " Width="1080" WindowStartupLocation="CenterScreen" WindowStyle="None" AllowsTransparency="True" Background="Transparent">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <Style x:Key="DataGridRowStyle1" TargetType="{x:Type DataGridRow}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DataGridRow}">
                        <Grid/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#E5E7EB" CornerRadius="30" MouseLeftButtonDown="Border_MouseLeftButtonDown">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <!--Menu icon-->
                <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Button Style="{StaticResource topButton}" Margin="20,28,0,35">
                    <Icon:PackIconMaterial Kind="Menu" Width="15" Height="15" />
                </Button>
                    
                <!--Left Menu-->
                <Border Grid.Row="1" Background="#623ed0" CornerRadius=" 0 50 0 30">
                    <DockPanel>
                        <Border Width=" 80" Height="80" Padding="3" Background="White" CornerRadius="100" Margin="20,-30,0,0" DockPanel.Dock="Top" >
                            <Ellipse RenderTransformOrigin="0.065,0.507">
                                <Ellipse.Fill>
                                    <ImageBrush ImageSource="C:\Users\doant\Pictures\Logo1.png"/>
                                </Ellipse.Fill>
                            </Ellipse>
                        </Border>
                        <TextBlock Text="Fashion Store" TextAlignment="Center" FontSize="22" FontWeight="Bold" Foreground="White" Margin="5,25,0,10" DockPanel.Dock="Top"/>
                        <TextBlock x:Name="txtRoleDisplay" Text="Role..." TextAlignment="Center" FontSize="14" FontWeight="SemiBold" Foreground="#d0bfff"  DockPanel.Dock="Top"/> 
                        <Separator Margin="25" Background="#7e5fdc" Height="0.8" DockPanel.Dock="Top"/>
                        <StackPanel DockPanel.Dock="Top">
                            <Button Style="{StaticResource MenuButton}">
                                <StackPanel Orientation="Horizontal" >
                                    <Icon:PackIconMaterial Kind="HomeVariant"  Style="{StaticResource MenuButtonIcon}"/>
                                    <TextBlock Text="Dashboard" />
                                </StackPanel>
                            </Button>
                            <Button Style="{StaticResource MenuButton}">
                                <StackPanel Orientation="Horizontal" >
                                    <Icon:PackIconMaterial Kind="TshirtCrew"  Style="{StaticResource MenuButtonIcon}"/>
                                    <TextBlock Text="Products" />
                                </StackPanel>
                            </Button>
                            <Button Style="{StaticResource MenuButton}">
                                <StackPanel Orientation="Horizontal" >
                                    <Icon:PackIconMaterial Kind="AccountGroup"  Style="{StaticResource MenuButtonIcon}"/>
                                    <TextBlock Text="Customers" />
                                </StackPanel>
                            </Button>
                            <Button Style="{StaticResource MenuButton}">
                                <StackPanel Orientation="Horizontal" >
                                    <Icon:PackIconMaterial Kind="ClipboardList"  Style="{StaticResource MenuButtonIcon}"/>
                                    <TextBlock Text="Orders" />
                                </StackPanel>
                            </Button>
                        </StackPanel>
                        <Button Style="{StaticResource MenuButton}" VerticalAlignment="Bottom" DockPanel.Dock="Bottom" Margin="15 0 15 20">
                            <StackPanel Orientation="Horizontal" >
                                <Icon:PackIconMaterial Kind="Logout"  Style="{StaticResource MenuButtonIcon}"/>
                                <TextBlock Text="Logout" />
                            </StackPanel>
                        </Button>
                    </DockPanel>
                </Border>
            </Grid>
            
                <!--Main Section-->
            <Grid Grid.Column="1" Margin="30 20 20 20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                
                <!--Seacrh bar and top icons-->
                <Grid Width="300" HorizontalAlignment="Left">
                    <TextBlock Margin="20 0" VerticalAlignment="Center" Foreground="#b0b9c6" IsHitTestVisible="False"
                        Panel.ZIndex="1" Text="Search here..." 
                        Visibility="{Binding ElementName=txtSearch, Path=Text.IsEmpty, Converter={StaticResource BoolToVis}}"/>
                    <TextBox Name="txtSearch" Style="{StaticResource textboxSearch}" TextChanged="txtSearch_TextChanged"/>
                    <Icon:PackIconMaterial Kind="Magnify" Width="15" Height="15" VerticalAlignment="Center" HorizontalAlignment="Right" 
                        Margin="0 0 15 0" Foreground="#b0b9c6"/>
                </Grid>

                <Button Style="{StaticResource topButton}" Margin="0 0 20 0" Background="#dcdcdc" HorizontalAlignment="Right">
                    <Icon:PackIconMaterial Kind="Cog"/>
                </Button>
                <Button Style="{StaticResource topButton}" Margin="0 0 55 0" Background="#dcdcdc" HorizontalAlignment="Right">
                    <Icon:PackIconMaterial Kind="Bell"/>
                </Button>
                
                <!--Main title-->
                <TextBlock Text="Products" Grid.Row="1" FontSize="32" FontWeight="SemiBold" Foreground="#121518" Margin="0 20 0 0" HorizontalAlignment="Left"/>
                
                <!--Add button-->
                <Button Style="{StaticResource addButton}" Click="AddButton_Click">
                    <StackPanel Orientation="Horizontal">
                        <Icon:PackIconMaterial Kind="Plus" VerticalAlignment="Center" Width="11" Height="11" Margin="0 1 8 0"/>
                        <TextBlock Text="Add New Product"/>
                    </StackPanel>
                </Button>
            
                <Separator Height="2" Grid.Row="3" Background="#dae2ea" Margin="0 -1 0 10"/>
                
                <!--Filter-->
                <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,5,0,5">
                    <TextBlock Text="Filter by Category:" FontSize="13" FontWeight="SemiBold" Foreground="#121518" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <ComboBox x:Name="cmbCategoryFilter" 
                              ItemsSource="{Binding Categories}" 
                              SelectedIndex="0"
                              SelectionChanged="CategoryFilter_SelectionChanged" 
                              Width="200" VerticalAlignment="Center"/>
                </StackPanel>
                <!--Data grid-->
                <DataGrid Grid.Row="5" x:Name="ProductsDatagrid"
                        ItemsSource="{Binding Products}" 
                        AutoGenerateColumns="False"
                        CanUserAddRows="False"
                        HeadersVisibility="Column"
                        GridLinesVisibility="None"
                        BorderThickness="0"
                        Background="Transparent"
                        ColumnHeaderStyle="{StaticResource CleanHeaderStyle}"
                        RowStyle="{StaticResource CleanRowStyle}"
                        CellStyle="{StaticResource CleanCellStyle}">

                    <DataGrid.Columns>
                        <DataGridTemplateColumn Width="auto">
                            <DataGridTemplateColumn.Header>
                                <CheckBox Margin="10,0,0,0"/>
                            </DataGridTemplateColumn.Header>
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="Product Name" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Border Background="#623ed0" CornerRadius="100" Width="30" Height="30" Margin="0,0,10,0">
                                            <Icon:PackIconMaterial Kind="TshirtCrew" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="White"/>
                                        </Border>
                                        <TextBlock Text="{Binding ProductName}" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn Header="Category" Width="auto" Binding="{Binding Category}"/>
                        <DataGridTextColumn Header="Price" Width="auto" Binding="{Binding Price, StringFormat='${0:N2}'}"/>
                        <DataGridTextColumn Header="Quantity" Width="auto" Binding="{Binding Quantity}"/>

                        <DataGridTextColumn Header="Description" Width="*" Binding="{Binding Description}"/>
                        <DataGridTemplateColumn Header="Operations" Width="auto">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Style="{StaticResource EditButton}" Click="EditButton_Click">
                                            <Icon:PackIconMaterial Kind="Pencil" Width="15" Height="15"/>
                                        </Button>
                                        <Button Style="{StaticResource DeleteButton}" Click="DeleteButton_Click">
                                            <Icon:PackIconMaterial Kind="Delete" Width="15" Height="15"/>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
                
                <!--Paging-->
                <Border Grid.Row="6" HorizontalAlignment="Right" BorderBrush="#dee4ec" BorderThickness="1" 
                        CornerRadius="5" Padding="5" Background="White" Margin=" 0 10 10 10">
                    <StackPanel Orientation="Horizontal" >
                        <Button Style="{StaticResource pagingButton}" Click="PrevButton_Click">
                            <Icon:PackIconMaterial Kind="ChevronLeft" Style="{StaticResource pagingButtonIcon}"/>
                        </Button>

                        <Rectangle Width="1" Fill="#dee4ec" Margin="3 7"/>

                        <Button Style="{StaticResource pagingButton}" Content="1" Click="PagingButton_Click"/>
                        <Button Style="{StaticResource pagingButton}" Content="2" Click="PagingButton_Click"/>
                        <Button Style="{StaticResource pagingButton}" Content="3" Click="PagingButton_Click"/>

                        <TextBlock Text="..." FontSize="15" FontWeight="Bold" Foreground="#6c7682" VerticalAlignment="Center" Margin="10 0"/>

                        <Button Style="{StaticResource pagingButton}" Content="7" Click="PagingButton_Click"/>
                        <Button Style="{StaticResource pagingButton}" Content="8" Click="PagingButton_Click"/>
                        <Button Style="{StaticResource pagingButton}" Content="9" Click="PagingButton_Click"/>

                        <Button Style="{StaticResource pagingButton}" Click="NextButton_Click">
                            <Icon:PackIconMaterial Kind="ChevronRight" Style="{StaticResource pagingButtonIcon}"/>
                        </Button>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>